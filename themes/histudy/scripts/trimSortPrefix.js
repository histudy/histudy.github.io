// var _ = require('lodash');
// var trimSortPrefix = function (name) {
//   var result = name.match(/^\d+_(.+)/);
//   return result ? result[1] : name;
// };
// hexo.extend.helper.register('trimSortPrefix', trimSortPrefix);
